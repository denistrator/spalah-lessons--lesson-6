$( document ).ready(function() {		
	$('.card__img-holder').height($('.card').height());
	$('.card__img-inner').height($('.card').height());
	$('.card__content').height($('.card').height());
	$('.card__content-inner').height($('.card').height());
});